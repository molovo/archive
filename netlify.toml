# Global settings applied to the whole site.
[build]
  publish = "_site"
  command = "gulp compile --env production && gulp compile:critical"

[context.branch-deploy]
  command = "gulp compile && gulp compile:critical"

[context.staging]
  command = "gulp compile --env staging && gulp compile:critical"